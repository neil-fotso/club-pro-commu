const mongoose = require("mongoose"); const User = require("../../models/User"); describe("User Model", () => { beforeAll(async () => { await mongoose.connect(process.env.MONGODB_URI, { useNewUrlParser: true, useUnifiedTopology: true, }); }); afterAll(async () => { await mongoose.connection.close(); }); beforeEach(async () => { await User.deleteMany({}); }); it("should create a valid user", async () => { const userData = { email: "test@example.com", password: "password123", pseudo: "testuser" }; const user = new User(userData); const savedUser = await user.save(); expect(savedUser.email).toBe(userData.email); expect(savedUser.pseudo).toBe(userData.pseudo); expect(savedUser._id).toBeDefined(); }); });
